# MapleCode

A terminal AI coding assistant (similar to Claude Code), written in Go.

## Run

```
go run ./cmd/maplecode
```

The first run will generate a config template at `~/.maplecode/config.yaml`.
Fill in your `api_key` (and adjust `protocol`, `model`, `base_url` as needed), then re-run.
